import {getRooms,getMenu,getReviews,getGallery,getAmenities} from './data.js';
export async function initAdmin(){document.querySelector('#app').innerHTML=`<section class="min-h-screen bg-stone-100 py-32"><div class="container-lr"><div class="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p class="section-subtitle">Operations</p><h1 class="section-title">Admin dashboard</h1></div><p class="text-sm text-stone-500">Connect Supabase through <code>window.LRINA_CONFIG</code> to enable authenticated management.</p></div><div id="admin-cards" class="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-5"></div><div class="mt-10 rounded-2xl bg-white p-7"><h2 class="font-serif text-3xl font-bold">Admin access</h2><p class="mt-3 text-stone-600">The management dashboard uses Supabase Auth and database CRUD. This static entry point keeps that backend boundary explicit and avoids storing credentials in the browser source.</p><p id="admin-status" class="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">Demo mode: configure Supabase before using live management operations.</p></div></div></section>`;const [rooms,menu,reviews,gallery,amenities]=await Promise.all([getRooms(),getMenu(),getReviews(),getGallery(),getAmenities()]);document.querySelector('#admin-cards').innerHTML=[['Rooms',rooms.length],['Menu items',menu.length],['Reviews',reviews.length],['Gallery images',gallery.length],['Amenities',amenities.length]].map(([label,value])=>`<div class="card-luxury p-6"><p class="text-sm text-stone-500">${label}</p><strong class="mt-2 block text-3xl text-stone-900">${value}</strong></div>`).join('');}
